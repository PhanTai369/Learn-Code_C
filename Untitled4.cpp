#include <stdio.h>

int main() {
    int num, digit1, digit2, digit3, sum;

    printf("Nh?p vào s? nguyên có 3 ch? s?: ");
    scanf("%d", &num);

    sum = digit1 + digit2 + digit3;

    printf("T?ng 3 ch? s? c?a %d là %d", num, num, sum);

    return 0;
}

