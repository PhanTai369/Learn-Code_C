#include<stdio.h>
#include<conio.h>
#include<math.h>
  int main(){
     float a,b,c; 	
  	printf("nhap vao 3 canh a b c : ");
  	scanf("%f%f%f", &a,&b,&c);
  	(pow(a,2)+pow(b,2)==pow(c,2)||pow(b,2)+pow(c,2)==pow(a,2)||pow(c,2)+pow(a,2)==pow(b,2))?
	  printf("tam giac do la tam giac vuong: "):
  		printf("la tam giac thuong ");
  		return 0;
  		}
